.env            # security - secrets go in docker-compose
venv/           # Docker installs its own dependencies
__pycache__/    # generated automatically
*.pyc           # compiled Python files
Dockerfile      # no need to copy itself
.dockerignore   # no need to copy itself
alembic/        # migrations run separately


